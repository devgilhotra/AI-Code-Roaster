# in real world projects we have a file named as requirements.txt 
# it is used to install all the packages instead of all installing one by one it installs all of them in one line command 

# to install


# pip install - r requiremnets.txt
# pip freeze > requirements.txt